The javac -cp command should be executed from:

inaenq\WEB-INF\classes\inaenq 

The jar cvf command should be eecuted from:

Workspace\inaenq\


the inaenq.rar already contains a ¢¥.war¢¥ file. Instead of compiling it again
it is possible to simply drop the .war file inside the tomcat/webapps/ directory
and let tomcat deploy it.